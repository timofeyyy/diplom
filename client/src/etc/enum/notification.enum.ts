export enum NotificationTypes {
    PASSWORD_UPDATE,
    AVATAT_UPDATE,
    USERNAME_UPDATE,
}

export enum NotificationMainTypes {
    EVENTS,
    REQUESTS,
    MENTIONS
}

export enum FriendStatusEnum {
    SUBSCRIBED,
    MUTUALLY,
    INCOMMING
}
